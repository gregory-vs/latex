# latex

Repositorio com projetos e templates LaTeX.

## Template reutilizavel

Use `template-latex/` como base para novos documentos. Ele segue o mesmo padrao
de organizacao do projeto `latex/latex`. A documentacao do template esta em
`template-latex/docs/README.md`.
